- defaults:
    link:
      relink: true

- clean: ['~']


- link:
  ${XDG_CONFIG_HOME}/nvim: config/nvim
  ${XDG_CONFIG_HOME}/tmux: config/tmux
  ${XDG_CONFIG_HOME}/fish: config/fish

- create:
  - ~/downloads