- defaults:
    link:
        create: true
        relink: true

- clean: ['~']

- link:
    ${XDG_CONFIG_HOME}/nvim: config/nvim
    ${XDG_CONFIG_HOME}/fish: config/fish
    ${XDG_CONFIG_HOME}/hypr: config/hypr
    ${XDG_CONFIG_HOME}/waybar: config/waybar
    ${XDG_CONFIG_HOME}/kitty: config/kitty
    ${XDG_CONFIG_HOME}/rofi: config/rofi

