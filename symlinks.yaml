- defaults:
    link:
      relink: true

- clean: ['~']


- link:
  ${HOME}/.config/nvim: config/nvim
  ${HOME}/.config/tmux: config/tmux
  ${HOME}/.config/fish: config/fish

- create:
  - ~/downloads